<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<insert id="add" parameterType="mvo">
	insert into member
	 values(member_seq.nextVal,#{id},#{password},#{name},#{address})
	</insert>
	<select id="list" resultType="mvo">
		select num, id, name from member order by 1 desc
	</select>
	<select id="detail" parameterType="int" resultType="mvo">
		select num,id,password,name, address from member
		where num=#{num}
	</select>
</mapper>  